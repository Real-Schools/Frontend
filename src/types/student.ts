export type StudentType = {
  id: string;
  email: string;
  firstName: string;
  lastName: string;
  otherNames: string;
  level: string;
  branchId: string;
  enrolledAt: string;
  avatar?: string;
  select?: boolean;
  highlight?: boolean;
  bio?: string;
  studentNumber?: string;
};
